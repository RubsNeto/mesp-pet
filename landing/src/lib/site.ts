// Configuração central de links e metadados do site.
// Mantém tudo num único lugar para evitar links divergentes pelos componentes.

/** Repositório no GitHub. */
export const REPO_URL = 'https://github.com/RubsNeto/mesp-pet';

/** Página do release mais recente (mostra os instaladores anexados). */
export const RELEASES_URL = `${REPO_URL}/releases/latest`;

/** Página de issues. */
export const ISSUES_URL = `${REPO_URL}/issues`;

/** Texto da licença. */
export const LICENSE_URL = `${REPO_URL}/blob/main/LICENSE`;

/** Perfil do autor. */
export const AUTHOR_URL = 'https://github.com/RubsNeto';

/** Versão atual do app (mantém em sincronia com package.json). */
export const APP_VERSION = '0.1.0';
